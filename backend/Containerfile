FROM node:20-alpine

WORKDIR /app

COPY package.json ./
RUN npm install --omit=dev

COPY . .

RUN mkdir -p /app/data

ENV PORT=3001
ENV DB_PATH=/app/data/focus.db

EXPOSE 3001

VOLUME /app/data

CMD ["node", "server.js"]
